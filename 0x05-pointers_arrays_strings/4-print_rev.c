#include "main.h"

/**
 * print_rev -> printing a string in reverse
 * @s: the string to be printed in rev
 * Return: void
 */
void print_rev(char *s)
{
	while (*str != '\0')
	{
		_putchar(*str++);
	}
		_putchar('\n');
}
