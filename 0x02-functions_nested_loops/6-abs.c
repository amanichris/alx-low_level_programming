#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the abs value of an int
 * @n: the int in question
 *
 * Return: the abs of an int
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
